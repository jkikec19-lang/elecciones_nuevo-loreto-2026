let db={};fetch("datos/consulta_db.json").then(r=>r.json()).then(x=>db=x);function buscar(){let d=document.getElementById("dni").value.trim();let r=document.getElementById("resultado");let p=db[d];if(!p){r.innerHTML="<h3 class=no>NO ES MIEMBRO DE MESA</h3><p>DNI no encontrado en el padrón.</p>";return;}r.innerHTML=`<h2>${p.nombre}</h2><p>DNI: ${d}</p><p>Mesa: ${p.mesa}<br>Orden: ${p.orden}</p><p>${p.miembro?"MIEMBRO DE MESA: "+p.condicion:"NO ES MIEMBRO DE MESA"}</p><p>${p.colegio}</p>`;}